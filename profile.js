// TODO: profile(src, validator([rule1, rule2]), dest)
/**
 * Profile a html with given rules
 * @param {string|ReadStream} src - input source
 * @param {TBD} validator - validator for an array of rules
 * @params {console|string|WriteStream} dest - output destination
 */
function profile(src, validator, dest) {
// asyncFetchHtml => parser => validator => dest


}


module.exports = profile;
